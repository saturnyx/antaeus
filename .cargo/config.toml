[target.'cfg(target_os = "vexos")']
rustflags = ["-Clink-arg=-Tvexide.ld", "--cfg", "getrandom_backend=\"custom\""]

[unstable]
build-std = ["std", "panic_abort"]
build-std-features = ["compiler-builtins-mem"]
